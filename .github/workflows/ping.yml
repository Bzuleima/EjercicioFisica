name: Keep Alive Streamlit

on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:
  
jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Ping Streamlit App
        run: curl https://ejerciciofisica-9z3khxamvsy3svuxascdjr.streamlit.app/
